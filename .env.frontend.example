# ─── Frontend Environment (Separate Deployment) ─────────────────────────────
# Copy this file to .env.local when running the frontend standalone.
# Point VITE_API_URL to wherever the backend is deployed.

VITE_API_URL=http://localhost:3000
