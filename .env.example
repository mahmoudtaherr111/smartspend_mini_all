# ─── Database ───
DATABASE_URL=mysql://smartspend:change-me@127.0.0.1:3308/smartspend
# Required by docker-compose.yml; use distinct, strong local development values.
MYSQL_DATABASE=smartspend
MYSQL_USER=smartspend
MYSQL_PASSWORD=change-me
MYSQL_ROOT_PASSWORD=change-me-too

# ─── Google OAuth ───
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://your-domain.com/api/auth/google/callback

# ─── JWT (min 32 chars) ───
JWT_SECRET=

# ─── Gemini AI ───
GEMINI_API_KEY=
GEMINI_MODEL_FREE=flash
GEMINI_MODEL_PRO=pro
GEMINI_MODEL_REPORTS=pro

# ─── Fireworks AI ───
FIREWORKS_API_KEY=

# ─── App URLs ───
NODE_ENV=production
PORT=3000
APP_URL=https://your-domain.com
# FRONTEND_URL=https://app.your-domain.com
# Additional trusted origins must be exact; separate multiple origins with commas.
# ALLOWED_ORIGINS=http://localhost:3000,https://your-static-tunnel.ngrok-free.dev
APP_TIMEZONE=Africa/Cairo
# Run cron jobs on the designated scheduler replica only.
ENABLE_CRONS=false
# Do not auto-connect the WhatsApp client unless explicitly required.
ENABLE_WHATSAPP=false

# ─── Owner (optional admin bootstrap) ───
OWNER_EMAIL=

# ─── Paymob (required for real revenue in production) ───
PAYMOB_API_KEY=
PAYMOB_INTEGRATION_ID=
PAYMOB_IFRAME_ID=
PAYMOB_HMAC_SECRET=
# NEVER set true in production:
# BILLING_SIMULATE=false

TRUST_PROXY=true
# The local ngrok agent connects from loopback, which is trusted automatically.
# Add only exact reverse-proxy peers when deploying behind another ingress.
# TRUSTED_PROXY_IPS=10.0.0.10,10.0.0.11
TRUSTED_PROXY_HEADER=x-forwarded-for
# Prefer a dedicated random secret in production (JWT_SECRET is only a fallback).
# RATE_LIMIT_KEY_SECRET=replace-with-at-least-32-random-characters
# Failed-login limits. The IP defaults account for mobile carrier NAT.
LOGIN_IP_MAX_FAILURES=50
LOGIN_IP_BURST_MAX_FAILURES=10
LOGIN_ACCOUNT_MAX_FAILURES=5
LOGIN_PAIR_MAX_FAILURES=5

# ─── Redis (required for voice calls in production) ───
REDIS_URL=redis://:your_redis_password@localhost:6379
# Allow in-memory cache fallback when Redis is unavailable in production (default: false)
# AI_ALLOW_MEMORY_CACHE_IN_PRODUCTION=false

# ─── Sentry (optional error tracking) ───
# SENTRY_DSN=

# ─── Firebase Cloud Messaging (FCM) ───
# For Backend (Firebase Admin SDK)
# You can download the service account JSON from Firebase Console -> Project Settings -> Service Accounts
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# For Frontend (Vite Client SDK)
# Get these from Firebase Console -> Project Settings -> General -> Your Apps (Web)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
# Get the Public VAPID Key from Firebase Console -> Project Settings -> Cloud Messaging -> Web Configuration -> Web Push certificates
VITE_FIREBASE_VAPID_KEY=

