# SmartSpend AI — دليل السياق البرمجي الموحد للذكاء الاصطناعي (Unified AI Codebase Context) 🧠💻

> **ملاحظة هامة للذكاء الاصطناعي (To Any Future AI Assistant):**
> هذا المستند يحتوي على المعمارية الكاملة والشرح التفصيلي العميق لمشروع **SmartSpend AI**، بما في ذلك هيكل الملفات بالكامل، منطق التدفقات، جداول قاعدة البيانات، وتفاصيل محرك التصنيف بالذكاء الاصطناعي. اقرأ هذا الملف لتفهم السيرفر والعميل وتفاصيل المعاملات دون الحاجة لقراءة الأكواد البرمجية من الصفر.

---

## 1. 📋 الكود التمهيدي للتوجيه المباشر (Copy-Paste Context Prompt)

انسخ النص التالي وضعه لأي نموذج ذكاء اصطناعي (مثل Claude, GPT, Gemini) قبل بدء التطوير أو تعديل الأكواد ليفهم المشروع بلحظة واحدة:

```text
أنت الآن المطور والخبير البرمجي الرئيسي لمشروع "SmartSpend AI".
لقد قمنا بإنشاء مستند سياقي شامل يصف كامل معمارية المشروع، وهيكل الملفات، وجداول قاعدة البيانات، ومحركات الذكاء الاصطناعي والرسائل البنكية بالتفصيل الممل في الملف التالي:
[smartspend_system_context.md](file:///e:/smartspend_V1_fixed/smartspend_system_context.md)
برجاء قراءة هذا الملف البرمجي بالكامل واستيعاب الأقسام التالية منه قبل الإجابة أو كتابة أي كود:
1. المعمارية الكلية (React + Hono + tRPC + Drizzle ORM + MySQL 8).
2. الفهرس التفصيلي لكل ملف في الباك إند والفرونت إند.
3. تفاصيل محرك التصنيف الهجين خماسي الطبقات (Zero-Token Cache + Rule Engine + Embedding + Gemini AI + Dispute Resolver).
4. نظام استقبال الرسائل التلقائي و Apple Pay للأجهزة.
5. جداول قاعدة البيانات الـ 20 والعلاقات الرابطة بينها.
أجبني دائماً مستنداً على القواعد والأنواع المشتركة في المجلد contracts، وحافظ على توافق الأنواع البرمجية (Type-Safety) لـ tRPC v11.
```

---

## 2. 🏛️ الرؤية العامة وهندسة النظام الكلية (System Architecture)

**SmartSpend AI** هو منصة مالية سلوكية متكاملة (Enterprise-Grade Behavioral Financial Platform) موجهة للبلدان العربية وتحديداً مصر، تهدف لتبسيط تسجيل وتتبع وإدارة وتحليل المصاريف والدخول عبر اللغة الطبيعية والصوت والرسائل البنكية التلقائية.

```
                  ┌──────────────────────────────────────────────┐
                  │              React Client (Vite)             │
                  │   - Tailwind CSS & shadcn/ui (50+ Components)│
                  │   - Recharts Graphics & next-themes          │
                  │   - tRPC Client (Typed HTTP requests)        │
                  └──────────────────────┬───────────────────────┘
                                         │ tRPC v11 Protocol
                                         ▼
                  ┌──────────────────────────────────────────────┐
                  │             Hono Server (Node 20)            │
                  │   - Context Auth Middleware (Dual Auth)      │
                  │   - tRPC Router & Endpoints (/api/trpc)      │
                  │   - Static & Public Asset Server             │
                  └──────────────────────┬───────────────────────┘
                                         │
                   ┌─────────────────────┴─────────────────────┐
                   ▼                                           ▼
      ┌─────────────────────────┐                 ┌─────────────────────────┐
      │   AI & Analytics Core   │                 │      Drizzle Engine     │
      │ - 7-Step Classif. Pipe  │                 │  - Connection Pool      │
      │ - Lifestyle Inf. Engine │                 │  - MySQL 8.0 Database   │
      │ - Report Personalization│                 │  - 20+ Tables & Relations│
      └─────────────────────────┘                 └─────────────────────────┘
```

*   **Front-Backend Type-Safety:** يتم مشاركة العقود والأنواع البرمجية (Types & Constants) بالكامل عبر المجلد `contracts/` لضمان حماية المدخلات والمخرجات دون الحاجة لإعادة كتابة الأنواع مرتين.
*   **TanStack Query integration:** جميع طلبات tRPC في الواجهة الأمامية يتم تغليفها بـ TanStack Query v5 للاستفادة من إدارة الكاش والتحديث التلقائي في الخلفية.

---

## 3. 📂 الفهرس الفني التفصيلي لملفات ومجلدات المشروع

هذا الفهرس يستعرض وظيفة كل ملف برمجي ومحتوياته بدقة متناهية:

### 📁 الباك إند (`api/`)

#### أ. الملفات الجذعية:
*   **`boot.ts`:** نقطة الانطلاق الأساسية للسيرفر. يهيئ تطبيق Hono، ويقوم بتركيب برمجيات CORS الوسيطة، ونظام الـ Logger للطلبات، ويوجه مسار تسجيل جوجل (`/api/auth/google`) والـ Callback التابع له، ويفعل مسار الـ tRPC الأساسي (`/api/trpc`) مستخدماً المعالج `fetchRequestHandler`.
*   **`router.ts`:** يجمع كل الـ Routers الفرعية في كائن رئيسي `appRouter` ويصدر النوع البرمجي الموحد `AppRouter` لتمكين الفرونت إند من استدعاء الدوال البرمجية بثبات الأنواع.
*   **`context.ts`:** يبني سياق الطلب أمنياً. يفحص أولاً وجود الكوكي الآمن `google_session` ويفك شفرته كـ JWT، وإذا لم يجد، يفحص ترويسة الطلب `Authorization: Bearer <Token>` ويفك تشفيرها للتحقق من هوية مستخدم الهاتف المحمول، ويطابق الجلسة في جدول `sessions` ويعيد كائن `UnifiedUser` متكامل الصلاحيات أو `null`.
*   **`middleware.ts`:** يحدد ويفحص مستويات الصلاحيات. يحتوي على:
    *   `publicProcedure`: لا يتطلب مصادقة (متاح للجميع).
    *   `authedProcedure`: يتطلب بالضرورة وجود مستخدم مسجل وجلسة نشطة.
    *   `proProcedure`: يحظر الطلب إذا لم يكن العضو مسجلاً في باقة `pro` أو `ultra` أو أدمن.
    *   `moderatorProcedure`: يمنح الصلاحية للمشرفين والمديرين فقط.
    *   `adminProcedure`: حظر مطلق لجميع الأدوار باستثناء الأدمن (`admin`).
*   **`server.ts`:** سكريبت إضافي يتيح تشغيل خادم تطوير Hono المستقل على منفذ مخصص بشكل منفصل عن الواجهات.

#### ب. الـ Routers المتخصصة (tRPC Routers):
*   **`auth-router.ts`:** توليد رابط تسجيل Google OAuth، واستقبال رمز الاستجابة (Callback Code)، واستبداله بتوكن جوجل للوصول، وسحب بيانات العضو، وتسجيل مستخدم جديد في جدول `users` وتوليد JWT وحفظه في كوكي HttpOnly.
*   **`local-auth-router.ts`:** يحتوي على دوال التسجيل بالهاتف (`register`) مع التحقق من كود الإحالة، وتسجيل الدخول بالرقم السري المشفر (`login`)، وجلب بيانات المستخدم الحالي (`me`)، وإصدار جلسة وحفظها في قاعدة البيانات.
*   **`local-auth-utils.ts`:** يحتوي على خوارزميات التشفير بـ `bcryptjs` (معدل 12 دورة)، دوال التحقق من أرقام الهواتف المحمولة المصرية (010, 011, 012, 015 بطول 11 رقم)، وتوليد أكواد الإحالة.
*   **`expense-router.ts`:** يدير المعاملات المالية بالكامل. يحتوي على استعلامات CRUD، بالإضافة لدوال الإحصائيات الشهرية الفائقة `getMonthlyStats` والسنوية `getYearlyStats` التي تجري حسابات مركبة للتصنيفات ومقارنات الشهور وتوزيع المصاريف على الساعات والأيام والاتجاهات المالية.
*   **`ai-router.ts`:** يربط الواجهات بمحركات الـ AI. يدير حدود استخدام الطلبات بالباقة، ويستدعي خط الأنابيب لتصنيف الحركات، ويولد النصائح المالية الشهرية والسنوية ومقارنات الفترات باستخدام Gemini API، ويتعلم من تعديلات الأعضاء.
*   **`analytics-router.ts`:** يتيح تسجيل وتجميع أحداث تحليل سلوك الإنفاق في قاعدة البيانات للـ Dashboard.
*   **`admin-router.ts`:** يحتوي على لوحة تحكم الإدارة الكاملة: إحصائيات النظام الكلية، جدول إدارة المستخدمين، إدارة جلسات تسجيل الدخول النشطة وإنهائها فوراً للأمان، سجل تتبع الأنشطة الكلي (Activity Log)، وإعدادات AI الكلية.
*   **`support-router.ts`:** تسيير تذاكر المساعدة والدعم الفني؛ إنشاء تذكرة، سرد التذاكر المفتوحة والمغلقة، تعيين التذكرة لمشرف، وتبادل الرسائل الفورية داخل التذكرة.
*   **`export-router.ts`:** يعتمد على مكتبة `xlsx` لسحب حركات المستخدم من قاعدة البيانات وبناء ملف Excel فاخر يحتوي على جداول الدخل والمصاريف والتحليلات جاهزاً للتحميل.
*   **`session-router.ts`:** جلب الجلسات النشطة للمستخدم وإتاحة خيار إلغاء الجلسات الأخرى (مثل ميزة فيسبوك وجوجل الأمنية).
*   **`pro-router.ts`:** معالجة ترقيات الاشتراكات وتتبع الفواتير والحدود الفنية للباقات Pro و Ultra.
*   **`ads-router.ts`:** إدارة حملات الإعلانات وعرضها وتعديلها وحذفها للأدمن، وتتبع نسب النقرات وتفاعل المستخدمين معها.
*   **`referral-router.ts`:** تتبع عمليات التسجيل بالإحالة وقبول الكود ومكافأة الأعضاء.
*   **`seo-router.ts`:** إدخال وتحرير معلمات محركات البحث وتوليد خريطة الموقع `Sitemap XML`.
*   **`profile-router.ts`:** تسيير الملف الشخصي التكيفي وحفظ إجابات العميل في الـ Onboarding وتعديل أهداف التوفير.

#### ج. مجلد المكتبات المساعدة الباطنية (`api/lib/`):
*   **`env.ts`:** التحقق التلقائي والصارم من وجود وصحة كافة متغيرات البيئة الحساسة في ملف `.env` عبر Zod Schema ومنع الخادم من العمل في حال وجود نقص أو خلل.
*   **`category-registry.ts`:** يحدد السجل الموحد للفئات الرئيسية والفرعية، وألوانها، وأيقوناتها، ويقوم بتطبيع الحركات وتصحيح كتابتها لضمان عدم وجود فئات مكررة أو تالفة في قاعدة البيانات.
*   **`classification-pipeline.ts`:** يحتوي على خط أنابيب المعالجة الهجين خماسي الطبقات لتحليل وتصنيف وتدقيق الحركات المالية بالكامل.
*   **`egyptian-dictionary.ts`:** قاموس مالي محلي ضخم للكلمات العامية المصرية ومطابقتها الفورية مع الفئات المناسبة لضمان الفحص فائق السرعة وبكلفة صفرية.
*   **`fuzzy-match.ts`:** دمج خوارزميات قياس المسافات اللغوية (Levenshtein Distance) للمطابقة الفئوية للكلمات قريبة الشبه وتوحيد الحروف العربية (مثل إزالة الهمزات وتوحيد الياء والألف المقصورة والتاء المربوطة).

#### د. مجلد محركات السلوك والذكاء السحابي المتقدمة (`api/services/`):
*   **`user-profile-service.ts`:** إدارة وإنشاء وتحديث الملف المالي والسلوكي الشخصي للمستخدم (`user_profiles`).
*   **`lifestyle-inference-engine.ts`:** محرك الاستخلاص السلوكي؛ يقوم بقراءة حركات ومصاريف المستخدم الممتدة واستنتاج صفاته المعيشية بدقة متناهية (هل هو متزوج، هل يدفع إيجار، هل لديه سيارة، هل يدخن، نمط الأمان المالي له) وتحديثها دورياً.
*   **`report-personalization-engine.ts`:** يقوم بدمج استنتاجات محرك السلوك مع بيانات Onboarding لبناء وتوليد تقارير مالية بالعامية المصرية مخصصة 100% لتلائم حياته المالية والاجتماعية الفريدة.
*   **`adaptive-question-engine.ts`:** مولد الأسئلة الذكي لشاشة Onboarding الترحيبية؛ يقرر السؤال التالي بدقة بناءً على إجابات الأسئلة السابقة لمنع تكرار الأسئلة غير المتوافقة مع حالة المستخدم.

---

### 📁 قاعدة البيانات (`db/`)
*   **`schema.ts`:** يحتوي على التعريف التفصيلي لـ 20+ جدول، مبني عبر قواعد Drizzle ORM للـ MySQL، مع الفهارس المخصصة (Indexes) ومفاتيح الربط الخارجية (Foreign Keys) وقواعد الحذف المترابطة (On Cascade Delete) لضمان سلامة وحماية سلامة قاعدة البيانات.
*   **`relations.ts`:** تعريف علاقات Drizzle Relations (علاقات One-to-Many و Many-to-One) لتمكين استدعاء البيانات المترابطة بكفاءة عالية (مثل جلب المستخدم مع كافة مصاريفه وجلساته في استعلام موحد ونظيف).
*   **`index.ts`:** يهيئ تجمع اتصالات MySQL (`mysql2 pool`) ويصدر نسخة محرك الاستعلامات الموحد `db` للاستخدام في جميع أنحاء النظام.
*   **`seed.ts`:** يقوم بتغذية قاعدة البيانات تلقائياً بالبيانات والخيارات الأولية والإعدادات عند التنصيب للمرة الأولى.
*   **`check-db.ts`:** سكريبت فحص سريع للتحقق اللحظي من سلامة الاتصال بقاعدة البيانات.
*   **`apply-migrations.ts`:** يطبق الترحيلات المتولدة يدوياً من تيرمنال الباك إند.

---

### 📁 العقود المشتركة والأنواع (`contracts/`)
*   **`constants.ts`:** يحتوي على كافة الثوابت العامة للنظام: أسماء الباقات، الفئات الافتراضية، معايير المدد الزمنية، والحدود الفنية.
*   **`errors.ts`:** فئة معالجة وتوحيد الأخطاء البرمجية `AppError` الموروثة من `Error`، مع توفير دوال مصانع (Factories) سريعة لتوليد الأخطاء الشائعة بترميزات الاستجابة الصحيحة للشبكة (مثل `badRequest`, `unauthorized`, `forbidden`, `notFound`, `internal`).
*   **`types.ts`:** يحتوي على كافة واجهات وأنواع TypeScript المشتركة بين الفرونت إند والباك إند لضمان ثبات الأنواع المطلق عبر تيار tRPC.

---

### 📁 الفرونت إند والواجهات (`src/`)

#### أ. صفحات التطبيق الكبرى (`src/pages/`):
*   **`Landing.tsx`:** صفحة التسويق والهبوط الفاخرة، تقدم شرحاً حركياً جذاباً لمميزات التطبيق والتقاط الحركات والذكاء الاصطناعي مع تأثيرات بصرية جذابة وبطاقات تفاعلية.
*   **`Home.tsx`:** الصفحة الجوهرية للتطبيق (Dashboard) للأعضاء المسجلين. تدير شاشة العرض الرئيسية وتنقسم داخلياً لعلامات تبويب متصلة لحظياً ومحفوظة الحالة للتنقل بين (نموذج التسجيل، الرسوم البيانية، التحليلات الذكية، التقويم المالي، الخريطة الحرارية).
*   **`Login.tsx`:** شاشة الدخول والتسجيل الأنيقة؛ تحتوي على بطاقات وتأثيرات بصرية متطورة والانتقال السلس بين تسجيل الدخول بـ Google أو المصادقة برقم الهاتف مع حقول تحقق تفاعلية.
*   **`AuthCallback.tsx`:** صفحة استلام التوجيه الآمن من جوجل، تلتقط الـ JWT token من معلمات الرابط، وتخزنه في كوكيز وجلسة العميل ليدخل فوراً للـ Dashboard.
*   **`Admin.tsx`:** واجهة الإدارة الكاملة المخصصة للأدمن والمراقبين؛ تنظم التبويبات المتعددة لإدارة الأعضاء والتحليلات الفورية وسجلات الأنشطة والجلسات والإعلانات وإعدادات النظام الحساسة.
*   **`Pro.tsx`:** شاشة تسويق وتفعيل الترقيات لباقات Pro و Ultra بأسلوب جذاب وبطاقات زجاجية تفاعلية ومقارنة دقيقة للميزات.
*   **`Support.tsx`:** شاشة الدعم الفني الخاصة بالأعضاء لمتابعة تذاكر المساعدة وإنشاء تذكرة دعم جديدة والتحدث مع الفنيين.
*   **`Settings.tsx`:** صفحة خيارات وتعديلات العضو؛ ربط الهاتف والحصول على كود الأتمتة التلقائي للأجهزة وتنزيل الـ Shortcut الجاهز.
*   **`UltraLounge.tsx`:** صالون مستخدمي باقة Ultra الفاخر، يعرض مميزات حصرية ومحتوى تفاعلي مصمم خصيصاً للنخبة.

#### ب. مكونات الواجهة المساعدة (`src/components/`):
*   **`Sidebar.tsx`:** شريط التنقل الجانبي الذكي؛ يستجيب لأحجام الشاشات المختلفة، يدعم الوضع الداكن والزجاجي، ويحوي قوائم الحركة والروابط الشخصية.
*   **`OnboardingCard.tsx`:** بطاقة ترحيب العميل الجديد؛ تشرح له خطوات إعداد التطبيق في جهازه وربطه الفوري مع الآيفون أو الأندرويد.
*   **`MobileHeader.tsx`:** الهيدر المخصص للجوال؛ يحتوي على زر الهامبرغر الجانبي المخصص لفتح القائمة الجانبية وإغلاقها بسلاسة.
*   **`AdBanner.tsx`:** يعرض الإعلانات المخصصة للأعضاء مع التتبع الذاتي للانطباعات الفردية والنقرات وإرسالها فوراً للباك إند.

#### ج. مكونات لوحة البيانات (`src/components/dashboard/`):
*   **`MonthlyStats.tsx`:** يعرض بطاقات إحصائيات الشهر الجاري بدقة متناهية متأثرة بأي حركة تسجيل جديدة (إجمالي الصرف، الدخل، الصافي، المتوسط اليومي، اليوم الأعلى صرفاً، الفئة الأعلى استهلاكاً).
*   **`ExpenseChart.tsx`:** رسوم بيانية ديناميكية تفاعلية بـ `Recharts` للتغير اليومي والاتجاه المالي للصرف والدخل، بالإضافة للتوزيع الفئوي للمصاريف.
*   **`CategoryPieChart.tsx`:** رسم بياني دائري يوضح نسب الصرف لكل فئة مالية مع ألوان مخصصة تعبر عن طبيعة الفئة.
*   **`UserIntelligencePanel.tsx`:** لوحة الذكاء المالي السلوكي؛ تعرض تقييم الاستقرار المالي والاستنتاجات التي استخرجها محرك السلوك (مثل تنبيهات زيادة الصرف على الترفيه أو توقعات السيولة النقدية بنهاية الشهر).
*   **`BehaviorInsights.tsx`:** يولد توصيات مالية وحلولاً توفيرية مكتوبة بلغة دافئة وبالعامية المصرية لمساعدة المستخدم على تقليل مصاريفه بناءً على عاداته الفعلية.
*   **`MonthlyCalendar.tsx`:** يعرض تقويماً شهرياً كاملاً، وتحتوي خانة كل يوم على القيمة الإجمالية للمصاريف ملونة بتدرج لوني يعبر عن حجم الصرف في ذلك اليوم لتسهيل التعرف البصري على الأيام الأكثر استهلاكاً.
*   **`SpendingHeatmap.tsx`:** خريطة حرارية تفصيلية تحلل الصرف بالساعة واليوم لكشف أوقات الإنفاق السريع المعتاد (مثل الشراء المتكرر صباحاً أو الطلبات الليلية).

#### د. مكونات التسجيل والحركات (`src/components/expenses/`):
*   **`ExpenseForm.tsx`:** نموذج الإدخال الفائق للعمليات؛ يحتوي على حقل الكتابة الطبيعية ومؤشر الصوت والتحليل بـ AI، ويعرض المعاملات المستخرجة فوراً في شاشة مراجعة أنيقة تتيح للمستخدم ضبط القيم وتعديل الفئات يدوياً قبل تأكيد الحفظ الفعلي لضمان أعلى مستوى من الدقة والتخصيص.
*   **`RecentExpenses.tsx`:** يعرض جدولاً تفاعلياً فائق التنسيق بالحركات المالية الأخيرة للعميل مع توفير خيارات البحث السريع والفلترة الفئوية وإجراء التعديل المباشر أو الحذف الفوري للحركة.

---

## 4. 🧠 قسم خاص ومفصل: نظام التصنيف بالذكاء الاصطناعي (AI Classification System)

يعتبر محرك التصنيف بالذكاء الاصطناعي (`classification-pipeline.ts`) القلب النابض والابتكار البرمجي الأبرز في مشروع **SmartSpend AI**. تم تصميمه ليكون فائق السرعة، فائق الدقة، وبأقل تكلفة تشغيلية ممكنة مستخدماً تكنولوجيا تصنيف هجينة خماسية المستويات.

```
                    المدخلات النصية أو الصوتية للمستخدم
                                    │
                                    ▼
                         [تنظيف النص وتوحيد الأحرف]
                                    │
                                    ▼
                      [استخراج المبالغ والعملات والأسماء]
                                    │
                                    ▼
                      [تحديد نية المعاملة: دخل أم صرف]
                                    │
                                    ▼
          ┌───────────────────────────────────────────────────┐
          ▼ 0 Tokens Cost                                     ▼ High Complexity
   [القاموس والبحث المحلي]                             [محرك القواعد الفوري]
          │                                                   │
          ├─ (ثقة ≥ 85%) ──► [Auto Save ✅]                   ├─ (تعدد معاملات أو تعقيد)
          │                                                   │
          ▼ (ثقة < 85% أو عدم تطابق)                            ▼
   [محرك التضمين الدلالي] ───────────────────────────────► [Gemini AI Layer]
    (Embedding Classifier)                                    │
          │                                                   ▼
          │                                           [Gemini 2.5 Flash]
          │                                                   │
          │                                           (في حال الفشل)
          │                                                   │
          │                                           [Gemini Fallback]
          │                                                   │
          ▼                                                   ▼
 ┌─────────────────────────────────────────────────────────────────────┐
 │                         [تقييم نسبة الثقة الكلية]                    │
 └──────────────────────────────────┬──────────────────────────────────┘
                                    │
         ┌──────────────────────────┼──────────────────────────┐
         ▼                          ▼                          ▼
  [ثقة ≥ 85%]                [ثقة 60% - 84%]             [ثقة < 60%]
 [حفظ تلقائي فوري ✅]       [شاشة المراجعة للمستخدم 📋]   [سؤال توضيحي ذكي ❓]
```

### ⚙️ التدفق البرمجي والتنفيذي لمحرك التصنيف (7 خطوات تفصيلية):

#### 1. توحيد وتنظيف النصوص (`text-normalizer.ts`):
*   يستقبل النص ويقوم فوراً بإزالة الرموز التعبيرية والمسافات الزائدة وعلامات التشكيل.
*   يوحد كتابة الأرقام: يحول الأرقام الهندية والعربية الشرقية (`١`, `٢`, `٣`...) إلى أرقام عربية غربية قياسية (`1`, `2`, `3`...).
*   ينظف اختصارات العملة: يحول `جنيه` و `ج.م` و `ج` إلى تطبيع موحد `EGP` لتبسيط القراءة اللاحقة.

#### 2. استخراج الكيانات المباشرة (`entity-extractor.ts`):
*   يستخدم تعبيرات نمطية متطورة (Regex Patterns) للبحث عن المبالغ واستخراجها كأرقام بدقة بالغة.
*   يحدد الأسماء المعروفة المذكورة في النص بمطابقتها مع قائمة جهات الاتصال الخاصة بالمستخدم (`knownPeople`).
*   يستخرج أسماء المتاجر والشركات والعلامات التجارية المعروفة (`merchants`).
*   **التعلم الفوري من الأقواس (Parenthesized Relation Extraction):** إذا كتب المستخدم عبارة مثل *"دفعت لفريدة 200 جنيه (أختي)"*، يلتقط النظام القوسين تلقائياً ويستنتج فوراً أن العضو المسجل باسم `فريدة` يرتبط بصلة قرابة `أخت` ويقوم بتحديث ملف المستخدم وحفظ الاسم تحت فئة `العائلة` تلقائياً وبكلفة صفرية.
*   **الالتقاط من جهات الاتصال الذاتية (Person Matching):** إذا ذكر المستخدم اسماً مسجلاً مسبقاً لديه في قائمة البروفايل (مثال: *"حولت 500 لمروان"*)، يطابق النظام الاسم تلقائياً ويجلب صلة القرابة الموثقة (مثال: "أخوك")، ويصنف المعاملة تلقائياً تحت الفئة الفرعية `مروان أخوك` ويعطي ثقة **100%** ويحفظها فوراً دون استهلاك توكنز AI واحدة!

#### 3. كشف نية المعاملة المادية (`intent-detector.ts`):
*   يحلل الكلمات الدلالية لتحديد اتجاه التدفق المالي:
    *   كلمات المصروفات (`expense`): *صرفت، دفعت، اشتريت، حاسبت، سددت، شحنت...*
    *   كلمات المدخولات (`income`): *قبضت، جالي، تحويل وارد، راتب، مرتب، مكافأة...*
    *   كلمات التحويلات (`transfer`): *حولت لـ، سحبت من، إيداع، فودافون كاش...*
    *   كلمات الاستثمار (`investment`): *اشتريت دهب، بورصة، شهادات بنكية، عقار...*

#### 4. محرك القواعد المحلي الفوري (Zero-Token Rule Engine):
*   يختبر النص المحلي بمطابقتة مع القاموس المصري (`egyptian-dictionary.ts`) ومجموع الكلمات المفتاحية الذاتية للمستخدم.
*   إذا كانت العبارة بسيطة وقصيرة وتحتوي على مبلغ واحد وكلمة مطابقة تماماً (مثال: *"100 جنيه فودافون كاش"* أو *"بنزين 200"*):
    *   يقوم بتصنيفها فوراً وإعطاء ثقة مرتفعة تفوق الـ **90%**.
    *   يتم حفظ المعاملة فوراً بشكل تلقائي وتخطي إرسالها للـ AI لتوفير استهلاك باقة الذكاء الاصطناعي والحفاظ على سرعة الاستجابة الفورية.

#### 5. محرك التضمين الدلالي الدقيق (Embedding Classifier Layer):
*   في حال لم تكن الكلمات مطابقة تماماً ولكنها قريبة المعنى، يستدعي النظام محرك التضمين الدلالي باستخدام نموذج التضمين `text-embedding-004`.
*   يحول العبارة إلى شعاع رياضي (Vector) ويطابقها مع متجهات الفئات المالية في قاعدة البيانات عبر حساب تشابه الجيب (Cosine Similarity).
*   في حال وجود تطابق دلالي وثيق (مثال: *"شحنت تانك العربية"* غير المطابقة للقاموس ولكنها قريبة دلالياً بنسبة عالية من فئة `مواصلات -> بنزين`):
    *   يصنفها فوراً مع إسناد ثقة دقيقة متناسبة، ويمنع الحاجة لاستدعاء نماذج التوليد الضخمة.

#### 6. طبقة الذكاء الاصطناعي السحابي المتخصصة (AI Classification Layer):
*   إذا كانت العبارة طويلة، معقدة، تحتوي على معاملات متعددة، سرد قصصي، أو كلمات شديدة الغموض (مثال: *"روحت قعدت مع العيال في كافيه ودفعت 350 جنيه وحاسبنا على التاكسي 50 جنيه وسلفت أحمد صاحبي 1000"*):
    *   يتم تجاوز محرك القواعد تلقائياً لحماية سلامة وتفكيك البيانات.
    *   يتم إرسال النص بالكامل مع Prompt أمني وفني مخصص ومحكم للغاية للهيئة المصرية العامية إلى نموذج **Gemini 2.5 Flash** (أو Groq).
    *   يتم حقن الـ Prompt بملخص ملف بروفايل المستخدم المالي وسلوكه وأسماء معارفه وأكواد كلماته كـ Context مغذٍ لضمان الفهم المطلق لسياق العبارة.
    *   **الاستجابة الهيكلية والـ Fallback:** يلتزم النموذج بإرجاع مصفوفة JSON منظمة تحتوي على العمليات المفككة بالقيم والفئات الرئيسية والفرعية ومستويات الثقة. في حال فشل المفتاح الأساسي للـ AI لأي سبب، يقوم السيرفر تلقائياً بالتبديل السريع للمفتاح البديل (`apiKey2`) أو الانتقال لنموذج الـ Fallback المحلي للحفاظ على سلامة الخدمة.

#### 7. محرك تسوية النزاعات والتدقيق (Dispute Resolution Engine):
*   يدير الخلافات اللغوية؛ فإذا صنف الـ AI المعاملة في فئة، بينما صنفها محرك التضمين الدلالي المحلي في فئة أخرى بثقة عالية تفوق الـ AI بفارق ملحوظ:
    *   يقوم المحرك بتحديد المعاملة كحالة نزاع وتعديل علامة `needsReview` كـ `true` مع حقن وسم النزاع دلالياً (`gemini_embedding_dispute`) وإجبار الواجهة على إظهارها في شاشة مراجعة المستخدم لحسم التصنيف يدوياً وتعليم النظام.

#### 8. سؤال التوضيح التفاعلي الذكي (Smart Clarification Interceptor):
*   إذا انخفضت نسبة الثقة الكلية للعملية عن **60%**، أو ذكر المستخدم اسماً مجهولاً تماماً ولم يذكر صلة قرابته في فئة العائلة (مثال: *"حولت 500 جنيه لمصطفى"* ومصطفى ليس من معارفه أو معجمه):
    *   يعلق النظام عملية الحفظ الفوري تلقائياً ويغير حالة الاستجابة إلى `clarify`.
    *   يقوم بصياغة سؤال تفاعلي فوري يظهر للمستخدم في صندوق المحادثة: *"مين مصطفى؟ (أخ، صاحب، مدير، الخ...) عشان أحفظه المرة الجاية."* بمجرد إجابة العميل يدوياً، يتم دمج الإجابة مع العبارة السابقة وحفظ مصطفى كمعرف دائم لديه في جدول المعارف الشخصية وبدء الحفظ التلقائي له مستقبلاً بذكاء فائق!

---

## 5. 📲 نظام القراءة التلقائية للرسائل البنكية و Apple Pay (Automated SMS Ingestion)

يحتوي النظام على واجهة برمجة تطبيقات ذكية للغاية مخصصة لاستقبال حركات رسائل البنوك تلقائياً وبأقل كلفة.

```
 [رسالة بنكية واردة للهاتف] ──► [تطبيق الأتمتة بالهاتف] ──► [طلب HTTP POST آمن بالتوكن]
                                                                        │
                                                                        ▼
 ┌──────────────────────────────────────────────────────────────────────┐
 │                      Hono Endpoint (/ai/parseSMS)                     │
 └──────────────────────────────────┬───────────────────────────────────┘
                                    │
                                    ▼
             [فحص التوكن الأمني ومطابقة جلسة العضو الفعالة]
                                    │
                                    ▼
 ┌──────────────────────────────────────────────────────────────────────┐
 │                   Rule-Based Ingestion Engine                        │
 ├──────────────────────────────────────────────────────────────────────┤
 │ - كشف وتحليل الكلمات المالية والدولية والمحافظ والعملات والمبالغ    │
 │ - قواميس البنوك المصرية: الأهلي، مصر، CIB، فودافون كاش، InstaPay... │
 │ - التفسير الفوري لحركات الشراء بـ Apple Pay كمصروف مالي مباشر         │
 └──────────────────────────────────┬───────────────────────────────────┘
                                    │
                                    ▼
             [حفظ العملية وتعديل الميزانية والإحصائيات الكلية]
```

### 📋 معايير وقواعد الفحص المعتمدة للرسائل:
1.  **محفظة Apple Pay:** تم تهيئة محرك القواعد للتعرف الفوري على عمليات الشراء اللاتلامسية وحركات الدفع بالهاتف (تأتي الرسائل البنكية عادة محتوية على رموز `Apple Pay` أو `Contactless payment` أو `خصم كارت`). يقوم النظام بقراءتها واستخراج القيمة النقدية مباشرة وحفظها كمصروف مالي لحظي دون أي حاجة لاستهلاك طلبات AI مدفوعة.
2.  **محافظ كاش وإنستاباي:** التعرف على العبارات المميزة للتحويلات الواردة (مثال: *تم استقبال مبلغ، تحويل وارد، تم إيداع*) وتسجيلها כـ `Income` (دخل) فوراً، أو عمليات السحب والخصم وتسجيلها كـ `Expense` (مصروف).
3.  **رسائل البنوك الإنجليزية والدولية:** تحليل العبارات المحتوية على صيغ المعاملات البنكية الدولية مثل `debited`, `credited`, `purchased`, `transferred` وربطها بالعملة المستخرجة مباشرة وتحويل قيمتها لعملة الحساب الافتراضية.

---

## 6. 🗄️ تفاصيل جداول قاعدة البيانات الـ 20 بالكامل (Database Schemas)

فيما يلي المواصفات الفنية وجداول قاعدة البيانات كما هي معرفة في ملف [schema.ts](file:///e:/smartspend_V1_fixed/db/schema.ts) بترميز Drizzle ORM:

```typescript
// 1. جدول مستخدمي جوجل OAuth
export const users = mysqlTable("users", {
  id: int("id").primaryKey().autoIncrement(),
  unionId: varchar("union_id", { length: 255 }).unique(),
  name: varchar("name", { length: 255 }).notNull(),
  email: varchar("email", { length: 255 }).notNull(),
  avatar: text("avatar"),
  role: varchar("role", { length: 50 }).default("user").notNull(), // user | moderator | admin
  plan: varchar("plan", { length: 50 }).default("free").notNull(), // free | pro | ultra
  referralCode: varchar("referral_code", { length: 100 }).unique(),
  aiTokensUsed: int("ai_tokens_used").default(0).notNull(),
  createdAt: timestamp("created_at").defaultNow(),
});

// 2. جدول مستخدمي المصادقة المحلية برقم الهاتف
export const localUsers = mysqlTable("local_users", {
  id: int("id").primaryKey().autoIncrement(),
  name: varchar("name", { length: 255 }).notNull(),
  phone: varchar("phone", { length: 50 }).unique().notNull(), // رقم هاتف مصري 11 رقم فريد
  password: varchar("password", { length: 255 }).notNull(),   // bcrypt hashed password
  role: varchar("role", { length: 50 }).default("user").notNull(),
  plan: varchar("plan", { length: 50 }).default("free").notNull(),
  referralCode: varchar("referral_code", { length: 100 }).unique(),
  aiTokensUsed: int("ai_tokens_used").default(0).notNull(),
  createdAt: timestamp("created_at").defaultNow(),
});

// 3. جدول العمليات المالية (المصاريف والدخل)
export const expenses = mysqlTable("expenses", {
  id: int("id").primaryKey().autoIncrement(),
  userId: int("user_id").notNull(),
  userType: varchar("user_type", { length: 50 }).notNull(), // oauth | local
  type: varchar("type", { length: 50 }).notNull(),         // income | expense | transfer | investment
  amount: decimal("amount", { precision: 12, scale: 2 }).notNull(),
  category: varchar("category", { length: 100 }).notNull(),
  subCategory: varchar("sub_category", { length: 100 }),
  rawText: text("raw_text"),                                // النص المدخل أو نص الرسالة البنكية الأصلي
  source: varchar("source", { length: 50 }).default("manual"), // manual | voice | ai_parsed
  date: timestamp("date").defaultNow().notNull(),
  createdAt: timestamp("created_at").defaultNow(),
});

// 4. جدول جلسات تسجيل الدخول النشطة للأعضاء
export const sessions = mysqlTable("sessions", {
  id: int("id").primaryKey().autoIncrement(),
  token: varchar("token", { length: 255 }).unique().notNull(),
  userId: int("user_id").notNull(),
  userType: varchar("user_type", { length: 50 }).notNull(),
  createdAt: timestamp("created_at").defaultNow(),
  expiresAt: timestamp("expires_at").notNull(), // جلسة ممتدة لـ 7 أيام
});

// 5. جدول بروفايل المستخدم الذكي ومعطيات Onboarding
export const userProfiles = mysqlTable("user_profiles", {
  id: int("id").primaryKey().autoIncrement(),
  userId: int("user_id").unique().notNull(),
  userType: varchar("user_type", { length: 50 }).notNull(),
  monthlyIncome: decimal("monthly_income", { precision: 12, scale: 2 }).default("0.00"),
  financialGoal: varchar("financial_goal", { length: 100 }), // saving | debt_repayment | investment | budgeting
  onboardingAnswers: json("onboarding_answers"),            // إجابات Onboarding الذكية
  aiInferredAttributes: json("ai_inferred_attributes"),     // سمات مستنتجة (أطفال، سيارة، سكن...)
  preferences: json("preferences"),                         // تفضيلات المستخدم الخاصة بالواجهات والتقارير
  createdAt: timestamp("created_at").defaultNow(),
});
```

*(تتوفر باقي الجداول الـ 15 بتراكيب مطابقة تماماً في ملف schema.ts لضمان الاتساق الهيكلي وتكامل البيانات).*

---

## 7. 🚀 إرشادات برمجية حاسمة للمطور والذكاء الاصطناعي (Strict AI Developer Rules)

إذا كنت تقوم بتعديل أو كتابة أي كود برمجي في هذا المشروع، يجب عليك الالتزام التام بالقواعد الصارمة التالية:

1.  **حماية وثبات الأنواع (Zero Any Policy):** لا تستخدم النوع `any` نهائياً في الواجهات أو الدوال. استخدم الأنواع المشتركة المصدرة من المجلد `contracts/types.ts`.
2.  **استدعاءات tRPC الآمنة:** عند جلب البيانات في الفرونت إند، استخدم استدعاءات tRPC المناسبة (مثال: `trpc.expense.getMonthlyStats.useQuery(...)`) بدلاً من استدعاءات `fetch` أو `axios` التقليدية لضمان أمان البيانات.
3.  **سلامة العمليات المالية (Decimal precision):** لا تقم بإجراء عمليات مادية في الباك إند باستخدام أرقام الفاصلة العائمة `float` العادية لتجنب أخطاء التقريب الحسابي البنكي. قم باستخدام الحزم البرمجية المخصصة أو الحساب الدقيق عبر ترميزات `decimal.js` أو القيم البنسية الصحيحة قبل التخزين في حقول `Decimal(12,2)`.
4.  **تطبيع وحوكمة الفئات (Category Normalization):** عند إضافة مصروف أو تصنيف، مرره دائماً عبر دالة `normalizeTransactionTaxonomy` المتاحة في `api/lib/category-registry.ts` لضمان كتابة وتسمية الفئة بالصيغة الموحدة المسجلة في السيرفر لمنع تشتت الرسوم البيانية للمستخدمين.
5.  **المصادقة وتفادي الـ Cascade:** عند كتابة استعلامات الحذف للمستخدم، تأكد من حذف كافة حركاته المادية، وجلساته النشطة، واستنتاجات السلوك، وسجلات تذاكره ومكافآته دفعة واحدة وبطريقة مترابطة أمنياً لحماية الخصوصية المطلقة للبيانات.

---

<p align="center">
  <strong>هذا المستند الشامل يمثل الفهم الكلي والموحد للذكاء الاصطناعي والمطورين لمشروع SmartSpend AI</strong><br/>
  <small>© 2026 SmartSpend AI — وثائق السياق البرمجي الفخمة والمغلقة</small>
</p>
