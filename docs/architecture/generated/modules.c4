// GENERATED by `npm run atlas` from the source code. Do not edit: the next run overwrites this file.
// The hand-written model sits one folder up: spec.c4, context.c4, views.c4 and flows/.

model {
  extend smartspend.api.modules {
    accounts = module 'Account lifecycle' {
      description 'Account deletion: purgeUserData removes every row a user owns, inside the caller’s transaction.'
      metadata {
        files '1'
        paths 'api/services/user-purge-service.ts'
      }
    }
    ai-actions = module 'AI action runtime' {
      description 'Actions the assistant proposes, such as recording an expense, updating a wallet or creating a goal: stored as pending drafts and executed only after the user confirms, with the artifacts shown in chat.'
      metadata {
        files '6'
        paths 'api/services/action-runtime/**'
      }
    }
    ai-governance = module 'AI usage and cost governance' {
      description 'Per-plan token limits and per-request caps, burst counting, AI cost metrics and the admin cost overview, and the check of model-written numbers against facts.'
      metadata {
        files '3'
        paths 'api/lib/ai-usage-policy.ts, api/services/ai-cost-analytics.ts, api/services/ai-cost-policy.ts'
      }
    }
    ai-insights = module 'AI insights and reports' {
      description 'The smart profile and the contacts it reads from user_contacts, adaptive onboarding questions, monthly behaviour snapshots, the personal context added to classification and report prompts, and the printable Pro report. The Gemini batch service only simulates its calls.'
      metadata {
        files '7'
        paths 'api/services/adaptive-question-engine.ts, api/services/lifestyle-inference-engine.ts, api/services/personal-context-builder.ts, api/services/pro-report-engine.ts, api/services/report-personalization-engine.ts, api/services/user-profile-service.ts, api/services/batch-ai-service.ts'
      }
    }
    ai-kernel = module 'AI Center kernel' {
      description 'Plans each AI Center turn without a model (intent, data needs, clarifying questions), packs the context, applies the capability registry and retrieval policy, words the answer with at most one model call, and logs traces.'
      metadata {
        files '11'
        paths 'api/services/ai-kernel/**'
      }
    }
    ai-memory = module 'AI memory' {
      description 'Long-term memory about each user: conversation capsules and running summaries, memories extracted by rules, optional Fireworks embeddings stored in MySQL with a backfill, and retrieval that scores memories by words and, when embeddings are on, by vector similarity. The Qdrant and quantized on-disk stores are used only by tests.'
      metadata {
        files '12'
        paths 'api/services/ai-memory/**'
      }
    }
    ai-providers = module 'AI provider access' {
      description 'Provider clients, the provider registry, model name mapping, routing and fallback chains, and the sealing of the provider keys saved in the admin console, which move to a new secret on their own.'
      metadata {
        files '13'
        paths 'api/lib/ai-gateway.ts, api/lib/provider-key-crypto.ts, api/lib/ai-provider-registry.ts, api/lib/ai-routing.ts, api/lib/llm-provider-chain.ts, api/lib/llm-router.ts, api/lib/groq-client.ts, api/lib/fireworks-client.ts, api/lib/fireworks-embedding-client.ts, api/lib/nvidia-client.ts, api/lib/deepseek-client.ts, api/lib/model-mapper.ts, api/lib/provider-health.ts'
      }
    }
    api-core = module 'API server core' {
      description 'Hono app and server entry points, request context, tRPC procedure builders and the root router.'
      metadata {
        files '5'
        paths 'api/boot.ts, api/server.ts, api/context.ts, api/middleware.ts, api/router.ts'
      }
    }
    api-routers = module 'tRPC routers and HTTP sub-apps' {
      description 'One file per router mounted in api/router.ts, plus the SMS Hono sub-app mounted in api/boot.ts.'
      metadata {
        files '24'
        paths 'api/*-router.ts'
      }
    }
    arabic-nlp = module 'Arabic and Egyptian text processing' {
      description 'Normalizers, dictionaries, Arabic number parsing, negation detection, fuzzy matching and speech-to-text corrections.'
      metadata {
        files '10'
        paths 'api/lib/arabic-number-parser.ts, api/lib/arabic-token-match.ts, api/lib/egyptian-dictionary.ts, api/lib/egyptian-names-dictionary.ts, api/lib/normalizer-v2.ts, api/lib/text-normalizer.ts, api/lib/unified-normalizer.ts, api/lib/negation-detector.ts, api/lib/fuzzy-match.ts, api/lib/stt-corrections.ts'
      }
    }
    auth = module 'Authentication and sessions' {
      description 'Password hashing, JWT session creation, session validation, login brute-force protection, the one module that changes a role, a plan or a session and invalidates the cached principal with it, and the phone challenges that prove a number over WhatsApp before a sign-up or a WhatsApp sign-in.'
      metadata {
        files '5'
        paths 'api/lib/access-control.ts, api/services/phone-challenge.ts, api/local-auth-utils.ts, api/lib/session-validation.ts, api/lib/login-protection.ts'
      }
    }
    billing = module 'Billing' {
      description 'Paymob checkout requests, webhook verification settings, the subscription grant that sets a user’s plan, and the per-plan catalog of limits and features read from the admin’s settings for the plans screen.'
      metadata {
        files '3'
        paths 'api/lib/paymob.ts, api/lib/subscription-service.ts, api/lib/plan-catalog.ts'
      }
    }
    classification = module 'Expense classification pipeline' {
      description 'smart-pipeline.ts and the modules it composes: financial events, admissibility, rules, muscle memory, embeddings, taxonomy, confidence calibration, decomposition, verification and the final per-item acceptance.'
      metadata {
        files '30'
        paths 'api/lib/smart-pipeline.ts, api/lib/rule-engine.ts, api/lib/muscle-memory.ts, api/lib/embedding-engine.ts, api/lib/generate-embeddings-cache.ts, api/lib/post-classifier-verifier.ts, api/lib/admissibility-gate.ts, api/lib/final-acceptance.ts, api/lib/classification-*.ts, api/lib/classifier-contract.ts, api/lib/confidence-*.ts, api/lib/correction-rules.ts, api/lib/category-registry.ts, api/lib/taxonomy-*.ts, api/lib/direction-governed-taxonomy.ts, api/lib/entity-extractor.ts, api/lib/narrative-decomposer.ts, api/lib/intent-detector.ts, api/lib/person-resolver.ts, api/lib/relationship-normalizer.ts, api/lib/amount-ledger.ts, api/lib/amount-linker.ts, api/lib/financial-event-plan.ts, api/lib/voice-intake-gate.ts, api/services/parser-trace.ts'
      }
    }
    classification-qa = module 'Classification benchmark helpers' {
      description 'Helpers used only by the classification benchmark and QA scripts: taxonomy assertions and simulated users.'
      metadata {
        files '2'
        paths 'api/lib/benchmark-taxonomy-assert.ts, api/lib/real-user-sim.ts'
      }
    }
    database = module 'Database schema and access' {
      description 'Drizzle schema, relations, storage classes and the MySQL connection pool.'
      metadata {
        files '5'
        paths 'db/**, api/queries/**'
      }
    }
    finance-semantic-layer = module 'Finance semantic layer' {
      description 'Answers factual finance questions from the ledger: period resolution, category matching, row aggregation, monthly report facts, proactive insights, chart artifacts and a per-user cache.'
      metadata {
        files '10'
        paths 'api/services/finance-semantic-layer/**'
      }
    }
    ingestion-parsers = module 'Ingestion parsers' {
      description 'Bank SMS parsing (rules first, then an AI parser with a per-user cache), the step that files a parsed message in the ledger (its category, the automatic save, and the suggestions kept over the monthly limit), and the generator of the personal iOS Shortcut.'
      metadata {
        files '4'
        paths 'api/lib/sms-ai-parser.ts, api/lib/sms-rule-parser.ts, api/lib/shortcut-generator.ts, api/services/sms-ledger.ts'
      }
    }
    jobs = module 'Scheduled job bodies' {
      description 'Job implementations scheduled from api/boot.ts.'
      metadata {
        files '6'
        paths 'api/jobs/**'
      }
    }
    ledger = module 'Ledger aggregates' {
      description 'Daily expense rollups (the delta applied inside every expense write, and reconciliation against the ledger), the expense_details side table, and salary-cycle month ranges.'
      metadata {
        files '4'
        paths 'api/services/expense-rollups.ts, api/services/financial-month.ts, api/services/budget-status.ts, api/services/debt-ledger.ts'
      }
    }
    notifications = module 'Notifications' {
      description 'Web push (VAPID) and Firebase Cloud Messaging delivery, scheduled and event-triggered notifications, budget alerts, activity nudges and the default templates.'
      metadata {
        files '2'
        paths 'api/notification-engine.ts, api/services/firebase.ts'
      }
    }
    platform = module 'Platform services' {
      description 'Environment validation, Redis client and cache keys, system settings, business time zone helpers, scheduled-job locks, the server logger (it redacts message text, codes, tokens and phone numbers, and writes a failed query without its values), Sentry error reporting under the same rule, and the record of AI provider key errors.'
      metadata {
        files '10'
        paths 'api/lib/env.ts, api/lib/log.ts, api/lib/error-reporting.ts, api/lib/redis-client.ts, api/lib/cache-keys.ts, api/lib/settings-cache.ts, api/lib/system-settings-registry.ts, api/lib/app-time.ts, api/lib/error-logger.ts, api/services/scheduler-lock.ts'
      }
    }
    receipt-parsing = module 'Receipt parsing' {
      description 'Receipt parsing with a vision model.'
      metadata {
        files '1'
        paths 'api/lib/receipt-image-parser.ts'
      }
    }
    security = module 'Request security' {
      description 'HTTPS redirection and security headers, rate limiting, allowed origins, client IP resolution, ownership checks, upload signature checks, bot protection and security logging.'
      metadata {
        files '11'
        paths 'api/lib/security-headers.ts, api/lib/rate-limit.ts, api/lib/origin-policy.ts, api/lib/http-origin-security.ts, api/lib/get-client-ip.ts, api/lib/ownership-guard.ts, api/lib/image-magic-bytes.ts, api/lib/admin-safe-fields.ts, api/lib/security-logger.ts, api/lib/anonymizer.ts, api/services/turnstile-service.ts'
      }
    }
    site-guide = module 'Site guide' {
      description 'How-to answers about using SmartSpend (linking SMS, cards and wallets, goals, reports) from a built-in knowledge base with embedding retrieval.'
      metadata {
        files '5'
        paths 'api/services/site-guide/**'
      }
    }
    storage = module 'File storage' {
      description 'File storage behind one driver interface (local disk or S3-compatible storage such as R2), plus the avatar service.'
      metadata {
        files '5'
        paths 'api/services/storage/**'
      }
    }
    voice = module 'Voice' {
      description 'Live voice calls. The rebuilt call in api/services/voice (Egyptian number speech and, as it lands, the gateway, the Gemini Live engine, the tools and the checks on what is said) is replacing the old WebSocket bridge (voice-call-service, voice-kernel). api/services/entitlements/voice.ts decides who may call, for how long and on which model, counting the Cairo month.'
      metadata {
        files '44'
        paths 'api/services/voice/**, api/services/entitlements/**, api/services/voice-kernel/**, api/services/voice-call-service.ts, api/services/voice-context-service.ts'
      }
    }
    whatsapp = module 'WhatsApp' {
      description 'Baileys WhatsApp client and the in-process OTP state it verifies against.'
      metadata {
        files '2'
        paths 'api/services/whatsapp-service.ts, api/services/otp-cache.ts'
      }
    }
  }

  extend smartspend.shared {
    contracts = module 'Shared contracts' {
      description 'Types, limits and billing plans shared by the web app and the API.'
      metadata {
        files '8'
        paths 'contracts/**'
      }
    }
  }

  extend smartspend.web.modules {
    web-account = module 'Account UI' {
      description 'Biometric lock and passkeys, the smart profile, business and people settings, and the push prompt.'
      metadata {
        files '9'
        paths 'src/components/auth/**, src/components/profile/**, src/components/settings/**, src/components/bank-sync/**, src/components/notifications/**'
      }
    }
    web-admin = module 'Admin UI' {
      description 'Admin console tabs: ads, audit log, raw SMS, learned rules, settings, WhatsApp, clarifications, notifications and the AI Center administration.'
      metadata {
        files '22'
        paths 'src/components/admin/**'
      }
    }
    web-ai = module 'AI Center UI' {
      description 'AI Center screens: the chatbot, the AI memory manager and the monthly AI report.'
      metadata {
        files '3'
        paths 'src/components/ai/**'
      }
    }
    web-bank-sync = module 'Bank sync UI' {
      description 'The bank sync screens: iPhone Shortcut and Android companion setup, the digital wallet view shown once a phone is connected, and the SMS webhook settings panel.'
      metadata {
        files '5'
        paths 'src/components/bank-sync/**, src/components/settings/SmsWebhookSettings.tsx'
      }
    }
    web-capture = module 'Expense entry UI' {
      description 'The expense form on the home screen: typed, spoken and photographed entries, the review and clarification steps, the offline text queue, local suggestions and offline input checks, and image compression before a receipt is uploaded.'
      metadata {
        files '4'
        paths 'src/components/expenses/ExpenseForm.tsx, src/components/expenses/ReceiptCapture.tsx, src/lib/clientRulesEngine.ts, src/lib/compress-image.ts'
      }
    }
    web-finance = module 'Finance UI' {
      description 'Home dashboard (summaries, calendar, charts, search, streaks), recent expenses and goals.'
      metadata {
        files '17'
        paths 'src/components/dashboard/**, src/components/expenses/**, src/components/goals/**, src/components/budgets/**, src/components/debts/**'
      }
    }
    web-growth = module 'Ads and SEO UI' {
      description 'The ad banner and SEO meta tags.'
      metadata {
        files '2'
        paths 'src/components/ads/**, src/components/seo/**'
      }
    }
    web-hooks = module 'Web hooks' {
      description 'React hooks for auth, admin, plan and ads data, push notifications, PWA lifecycle, biometrics, navigation, keyboard and haptics.'
      metadata {
        files '20'
        paths 'src/hooks/**'
      }
    }
    web-insights = module 'Insights UI' {
      description 'The monthly AI insights view shown in the AI Center’s analysis tab.'
      metadata {
        files '1'
        paths 'src/components/insights/**'
      }
    }
    web-lib = module 'Web utilities' {
      description 'Client utilities (back-button handling, biometric auth, client rules engine, image compression, financial taxonomy, query persistence, transaction display), src/types and loose files at the root of src/.'
      metadata {
        files '9'
        paths 'src/lib/**, src/types/**, src/*.ts'
      }
    }
    web-pages = module 'Web pages' {
      description 'Route-level page components lazy-loaded by src/App.tsx.'
      metadata {
        files '15'
        paths 'src/pages/**'
      }
    }
    web-shared = module 'Shared web components' {
      description 'Shell components at the root of src/components: sidebar, notification bell, onboarding card, product tour and loading skeleton.'
      metadata {
        files '5'
        paths 'src/components/*.tsx'
      }
    }
    web-shell = module 'Web app shell' {
      description 'Entry point, providers, layout, route guards and PWA or native integration.'
      metadata {
        files '18'
        paths 'src/main.tsx, src/App.tsx, src/providers/**, src/pwa/**, src/components/layout/**, src/components/routing/**, src/components/pwa/**'
      }
    }
    web-ui-kit = module 'UI primitives' {
      description 'shadcn and Radix UI primitives in src/components/ui/.'
      metadata {
        files '55'
        paths 'src/components/ui/**'
      }
    }
    web-voice-call = module 'Live voice call UI' {
      description 'The live voice call in the app. The rebuilt call: a store any screen can start the call from (src/lib/voice/call-store.ts), which keeps it running across pages; microphone capture filtered down to 16 kHz with speech detection that sends audio only while the user speaks; the /api/voice/v2 socket client that resumes a dropped call; playback of the assistant’s voice; and the call screen with its cards, the Home button and the AI Center tab (src/components/voice). The old call screen and its hook (AIVoiceCall.tsx, useVoiceCall.ts on /api/voice/live) stay for users outside the rollout.'
      metadata {
        files '15'
        paths 'src/components/ai/AIVoiceCall.tsx, src/hooks/useVoiceCall.ts, src/components/voice/**, src/hooks/useVoiceCallEntry.ts, src/lib/voice/**'
      }
    }
  }
}
